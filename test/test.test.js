const assert = require("assert");

describe("func", () => {
  it("should", () => {
    assert.ok(true);
  });
});

describe("async", async () => {
  it("should", () => {
    assert.ok(true);
  });
});
